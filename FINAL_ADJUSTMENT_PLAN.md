# MUCViS 最終調整計画 — 実装側 / モデル側

これまでの実装コード（`mucvis_cn_partial_forUWB.cpp` / `mucvis_rn_command.cpp` /
`mucvis_camn_video_capture_command_pipethread_forUWB.cpp`）の読み込みと、
実験ログ（`IPT_0903-1_*`, `IPT_0518_1M_2_*`）の解析を踏まえた、最終調整の
優先順位付きまとめ。

---

## 実装側の最終調整案

### 優先度1（最優先）: RSSI・測距値のパケット相乗り

現状、各ノードは`packet.set_distance(my_dist_slot, g_current_distance_cm.load())`
という形で、自分の測距値を映像/ダミーパケットのヘッダに埋め込んでCtlNまで
届けている。**これと全く同じパターンで`packet.set_rssi()`を追加する。**

- `distance_reader_thread`と対になる`rssi_reader_thread`を各ノードに実装
- ローカルな受信フレーム統計（`iw dev wlan0 station dump`等）を定期的に
  読み、`g_current_rssi`のようなatomic変数にキャッシュ（新規パケット送信は
  発生しないため通信負荷はほぼゼロ）
- `packet.set_distance()`と並べて`packet.set_rssi(my_dist_slot, ...)`を呼ぶ

これが入って初めて、Layer1/Layer2を理論値ではなく実測RSSIで直接較正できる。

### 優先度2: 移動状態フラグの追加

`IPT_0518_1M_2`ログの解析で、**同一距離(L=90cm)でも走行中と静止後で映像ロス率が
5倍以上違う**（往路382件/分 vs 復路0件/分）ことが判明した。この現象を次回以降
確定的に切り分けるため、「現在ロボットが物理的に動いているか、目標位置に到達して
静止しているか」を示すフラグをHealthMetricsまたは新規イベントとして記録する。
モーター制御プロセス（`main lidar`）側の情報を`/tmp/`経由か共有メモリで通信
プロセスにも渡す形が、既存の設計パターン（`robot_current_distance.txt`と同じ）
と一貫性がある。

### 優先度3: 目標値と実測値の乖離検知

「コマンドで指示した目標距離」と「実際の測距値」の差が閾値を超えたら
`Position_Anomaly`のようなイベントをログに残す。これがあれば、ロスの原因が
電波環境なのか、キャタピラの空転のような機械的な原因なのかを、推測ではなく
直接判定できる。

### 優先度4: ロス率算出方法の統一

現在、リアルタイムの`HealthMetrics LossRate`（ウィンドウ集計）、オフライン
解析でのシーケンス突き合わせ、`Command_lost`（ACKジャンプ検知）という3種類の
指標が混在している。`Command_lost`はDICOMO2026論文の公表値と一致しない
（実測28%台 vs 公表0.1%）ことを確認済みで、多段ホップ中継時のACK値の
一時的な前後関係の乱れを誤検知しやすい。**シーケンス番号でのSend/Recv突き合わせ
に一本化**し、`Command_lost`は診断用途に留める。

### 優先度5: ホップ切替の自動化

現状の`generate_command_from_input`は`std::getline(std::cin, ...)`による
**操作員の手入力トリガー**であり、自動的な連続監視ではない。ハイブリッド制御
（あるいは自動距離ベース制御）を実際に試すには、測距値を定期的に読んで
`k=ceil(L/R)`を自動計算するループに置き換える必要がある。

### 優先度6（余裕があれば）: IPT間隔の実験パラメータ化

`ipt_interval`は既に設定ファイルから読めるため、複数のIPT値（1.0/1.2/1.5ms等）
を振った実験を追加し、「IPT=1.2msはH=3で常に警戒域」というモデル予測を実測で
直接検証する。

---

## モデル側の最終調整案

### 優先度1: 待ち行列モデルとIPT衝突モデルの統合

`mucvis_prop/queueing.py`（キュー溢れ）と`layer3.py`（衝突マージン）を
`dynamic_sim.py`のトラジェクトリ生成に統合し、1回の走行予測で両方の破綻要因を
同時に判定できるようにする。実装側の対応を待たずに着手可能。

### 優先度2: ログの直接読み込み対応

今回作成した正規表現パーサ（`T=...Ev=...`形式の生ログを読む処理）を
`mucvis_prop`に正式なモジュールとして組み込み、`mucvis_sim.html`のリプレイ
機能が生ログファイルを直接読めるようにする。実装側の対応を待たずに着手可能。

### 優先度3: 移動状態による追加劣化項（新しい知見の反映）

同一距離でも走行中は静止時よりロス率が悪化するという発見を受け、モデルに
「移動中フラグ」に応じた追加損失項を用意する。ただし根拠が現状1回の観測のみ
で弱いため、**係数0（オフ）をデフォルトにしたプレースホルダとして先に実装し**、
実装側優先度2のログが揃い次第、真の値を較正する2段階方式にする。

### 優先度4: デッドスポット・測定再現性σの較正データ差し替え

これまでの残差解析（N=7〜13点、統計的に弱い）に代え、RSSI付きの新しいログが
手に入り次第、高密度な実測点からσと周期性を再推定する。実装側優先度1が
前提条件。

### 優先度5: モードAの収縮ロジックを可変パラメータに

「ヒステリシスなし」は実装コードで確認済みの事実だが、これを固定にせず、
収縮側にどれだけ余裕（不感帯）を足せば安定するかを振れるパラメータとして
残す。「RSSI監視より先に距離だけのヒステリシス追加を試す価値がある」という
結論をいつでも再現できるようにする。

### 優先度6（余裕があれば）: 操作員トリガー遅延のモデル化

実装側優先度5（自動化）が実施されない場合、モデル側に「目標位置更新から
実際にコマンドが打たれるまでの遅延」を明示的なパラメータとして加え、
現状の実装をより正直に予測する。

---

## 進め方の指針

実装側の優先度1・2（RSSI相乗り・移動状態フラグ）が入った実験ログが1本
取れれば、モデル側の優先度1〜4を一気に本較正できる。モデル側の優先度1・2
（待ち行列統合・ログ直接読み込み）は実装の進捗を待たずに今すぐ着手できる。

## これまでの実験ログとの対応

| ログ | 内容 | 主な発見 |
|---|---|---|
| `IPT_0903-1_*` | 机上・試験用、RN待機のみ | VideoQueueSizeがほぼ常に0（良好な電波環境下では待ち行列モデルの予測通り溢れない）、制御コマンドロスが3ホップ遷移直後にのみ集中発生（H=3のIPT余裕の薄さと整合） |
| `IPT_0518_1M_2_*` | DICOMO2026発表時実験、実際に往復走行 | 映像ロス率2.347%（論文Table4の2.32%とほぼ完全一致、同一データと確認）、キュー溢れはL=130cm（最遠）でのみ発生、映像ロス率自体はL=90cmの往路が最悪（5倍以上）で復路はゼロ＝静的な距離依存モデルでは説明できない、走行中の機械的要因（キャタピラ空転等）の関与が疑われる |
